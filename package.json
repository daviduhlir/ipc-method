{
  "name": "@david.uhlir/ipc-method",
  "version": "1.0.22",
  "description": "IPC Method calling between forks and master",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "build:release": "rimraf ./dist && tsc --p tsconfig.release.json",
    "prettier-check": "prettier-check --config .prettierrc.json 'lib/**/*.ts'",
    "prettier-format": "prettier --config .prettierrc.json 'lib/**/*.ts' --write",
    "release": "./scripts/release.sh"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/daviduhlir/ipc-method.git"
  },
  "keywords": [
    "server",
    "node",
    "ipc",
    "promises"
  ],
  "author": "David Uhlíř",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.11.9",
    "prettier": "^2.6.2",
    "rimraf": "^2.6.2",
    "typescript": "^5.5.4"
  }
}
